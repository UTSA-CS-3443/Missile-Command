package controller;

import javafx.event.EventHandler;
import javafx.scene.input.MouseEvent;

/**Game credits which plays on exit. We might want to do this at intro instead. Either way
 * to be added near end of project (don't need this in program at all for demo)
 *
 * @author Gregory
 *
 */

public class CreditsController implements EventHandler<MouseEvent>{

	public CreditsController() {
		super();
	}

	@Override
	public void handle(MouseEvent event) {
		// TODO Auto-generated method stub

	}
}