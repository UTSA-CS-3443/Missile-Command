package controller;

import javafx.event.EventHandler;
import javafx.scene.input.MouseEvent;

/**Used for additonal information and controls, displayed at bottom of game screen. Suggest three parts:
 * General game info and menu buttons, Production controls, and Research controls. Don't really need this
 * for basic implementation ingame menu can be called by escape key.
 *
 * @author Gregory
 *
 */
public class GamePanelController implements EventHandler<MouseEvent>{

	public GamePanelController() {
		super();
	}

	@Override
	public void handle(MouseEvent event) {
		// TODO Auto-generated method stub

	}
}
